int main() {
    TrainableNNUE nnue(4, 10); // Example initialization
    auto dataset = loadTrainingData(); // Load your training data

    trainNetwork(nnue, dataset, 100, 0.01); // Train for 100 epochs with a learning rate of 0.01

    return 0;
}
